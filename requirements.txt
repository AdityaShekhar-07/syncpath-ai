# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0

# UI
streamlit>=1.35.0

# Agents (optional — comment out if not using CrewAI)
crewai>=0.63.0

# GTFS real feed parsing (optional — comment out if using simulation only)
# gtfs-realtime-bindings>=1.0.0

# Testing
pytest>=8.2.0
